/// out: "3"


import std::vector::OldVector


def foo(v: &OldVector) {
    v.push(null)
}

def main() {
    let my_vector = OldVector::new()
    foo(my_vector)
    foo(my_vector)
    foo(my_vector)

    println(`{my_vector.size}`)
}