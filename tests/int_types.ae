/// out: "pass"

def main() {
    if sizeof(i8) != 1 println("fail")
    if sizeof(i16) != 2 println("fail")
    if sizeof(i32) != 4 println("fail")
    if sizeof(i64) != 8 println("fail")

    if sizeof(u8) != 1 println("fail")
    if sizeof(u16) != 2 println("fail")
    if sizeof(u32) != 4 println("fail")
    if sizeof(u64) != 8 println("fail")

    println("pass")
}