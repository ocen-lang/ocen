/// runfail: '--------------------------------------------------------------------------------\ntests/assert.oc:6:12: Assertion failed: `x == 2`\n  Message: x should be 2\n--------------------------------------------------------------------------------'

def main() {
    let x = 1
    assert x == 1
    assert x == 2, "x should be 2"
}