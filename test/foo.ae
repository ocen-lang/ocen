import test::ns1::ns2
import bar
import bar::baz
import bar::baz::do_baz

namespace foo1 {
    def do_foo(): i32 {
        return 1
    }
}

def get_one(): i32 {
    return foo1::do_foo()
}

def test1(a: &i32) { 
    return
}

def test2(): &char {
    print("chris is %s", "cool")
    return "chris is cool"
}

def sum(a: i32, b: i32 = get_one()): i32 {
    return a * b + a
}

def returns_bool(): bool {
    return true
}

def do_foo(): i32 {
    let x = 1
    let y: &char  = "pydis"
    if x < x+1 < x+2 {
        let x = 10
    }
    print("x is %d", x)
    sum(a: 5)
    ns2::do_ns2()
    bar::baz::do_baz()
    baz::do_baz()
    return do_baz()
}